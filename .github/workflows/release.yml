# .github/workflows/release.yml
name: Release

on:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: write

jobs:
  release:
    name: Build & Release
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Resolve codename
        id: meta
        run: |
          declare -A CODENAMES
          CODENAMES["v0.0.1"]="not-good-enough-to-get-a-codename"
          CODENAMES["v0.1.0"]="now-with-kerberosity"
          CODENAMES["v0.2.0"]="just-use-yolo-mode"
          CODENAMES["v0.3.0"]="the-one-where-files-in-root-directory-get-downloaded-too"

          TAG="${{ github.ref_name }}"
          CODENAME="${CODENAMES[$TAG]:-unknown}"
          echo "codename=$CODENAME" >> $GITHUB_OUTPUT

      - name: Build all targets
        run: |
          mkdir -p dist
          make release-all \
            VERSION=${{ github.ref_name }} \
            CODENAME=${{ steps.meta.outputs.codename }} \
            BUILDTIME=$(date -u +'%Y-%m-%dT%H:%M:%SZ')

      - name: SHA256 checksums
        working-directory: dist
        run: sha256sum crawlofduty-* > checksums.txt

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: "${{ github.ref_name }}"
          body: |
            ## ${{ github.ref_name }}

            | File | Platform |
            |------|----------|
            | `crawlofduty-linux-amd64` | Linux x86_64 |
            | `crawlofduty-linux-arm64` | Linux ARM64 |
            | `crawlofduty-windows-amd64.exe` | Windows x86_64 |
            | `crawlofduty-darwin-arm64` | macOS Apple Silicon |
          files: |
            dist/crawlofduty-*
            dist/checksums.txt
          generate_release_notes: true
